name: Integration ML - Resume Training (Production)

on:
  schedule:
    - cron: "0 3 * * *"  # 2. test_resume_training.py
  workflow_dispatch:

jobs:
  test:
    uses: NeuracoreAI/shared-workflows/.github/workflows/integration-ml-test.yaml@main
    with:
      environment: production
      test-path: tests/integration/ml/test_resume_training.py
    secrets: inherit
