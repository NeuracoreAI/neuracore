name: Integration ML - Dataset Datatype Validation (Production)

on:
  schedule:
    - cron: "0 7 * * *"  # 6. test_dataset_datatype_validation.py
  workflow_dispatch:

jobs:
  test:
    uses: NeuracoreAI/shared-workflows/.github/workflows/integration-ml-test.yaml@main
    with:
      environment: production
      test-path: tests/integration/ml/test_dataset_datatype_validation.py
    secrets: inherit
