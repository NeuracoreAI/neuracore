name: Integration ML - Dataset Mutation Training (Production)

on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

jobs:
  test:
    uses: NeuracoreAI/shared-workflows/.github/workflows/integration-ml-test.yaml@main
    with:
      environment: production
      test-path: tests/integration/ml/test_dataset_mutation_training.py
    secrets: inherit
