name: Integration ML - Failure Reporting (Staging)

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  test:
    uses: NeuracoreAI/shared-workflows/.github/workflows/integration-ml-test.yaml@main
    with:
      environment: staging
      test-path: tests/integration/ml/test_failure_reporting.py
      data-daemon-diagnostics: true
    secrets: inherit
