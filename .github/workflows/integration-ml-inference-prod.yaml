name: Integration ML - Inference (Production)

on:
  schedule:
    - cron: "0 4 * * *"  # 3. test_inference.py
  workflow_dispatch:

jobs:
  test:
    uses: NeuracoreAI/shared-workflows/.github/workflows/integration-ml-test.yaml@main
    with:
      environment: production
      test-path: tests/integration/ml/test_inference.py
    secrets: inherit
